'use strict';

describe('Controller: SoloCtrl', function () {

  beforeEach(module('qodex'));

  var SoloCtrl;

  beforeEach(inject(function ($controller) {
    SoloCtrl = $controller('SoloCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
